# Model v0.6 — Validated Checkpoint

Continuous demand-weighted readiness is the locked reduced-model endpoint.

Validated checkpoint commit before the E2 production workflow:

`ca9b378f6187f0f9f2024b423c6b99def7921916`

GitHub Actions run `33780480187` completed successfully and verified:

- Models v0.1-v0.6 Octave unit suites;
- E1 raw/summary smoke pipeline;
- E2 raw-data smoke (`R=1`, 10 concentration cells);
- E2 Python summary smoke.

The E2 screening workflow was introduced only after this checkpoint.