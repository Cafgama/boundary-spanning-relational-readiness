# Step 30 — Mini heatmap figure generation

## Purpose

Generate a manuscript-facing Python heatmap from the audited rerun_v2 mini-heatmap CSV exports.

The figure is intended as a visual synthesis of two already-tested mechanisms:

1. translation capability (`pi_BS`);
2. boundary-spanner role capacity/workload (`b` and load per spanner).

It does not replace the final-core, translation-grid, or workload-grid evidence blocks, and it does not introduce a new causal identification strategy.

## Source data

Default input:

```text
results/figure_data/rerun_v2/mini_heatmap/mini_heatmap_matrix_RMST.csv
```

This CSV is generated by:

```octave
analyze_mini_heatmap_results
```

Expected production design:

```text
pi_BS_grid = [0.55, 0.60, 0.70]
b_grid     = [1, 2, 6]
NG         = 50
NT         = 50
T_max      = 50000
theta      = 0.80
q          = 0.80
```

## New files

```text
scripts/rerun_v2/plot_mini_heatmap.py
tests/rerun_v2/test_mini_heatmap_figure_script.py
docs/step30_mini_heatmap_figure.md
```

## Outputs

Default output directory:

```text
figures/rerun_v2/mini_heatmap/
```

Expected outputs:

```text
figures/rerun_v2/mini_heatmap/mini_heatmap_rmst_heatmap.png
figures/rerun_v2/mini_heatmap/mini_heatmap_rmst_heatmap.pdf
figures/rerun_v2/mini_heatmap/mini_heatmap_rmst_heatmap_caption.txt
```

## How to run

From PowerShell:

```powershell
cd C:\Users\cafga\boundary-spanning-relational-readiness
git pull
python tests\rerun_v2\test_mini_heatmap_figure_script.py
python scripts\rerun_v2\plot_mini_heatmap.py
```

Expected test message:

```text
ALL RERUN V2 MINI HEATMAP FIGURE TESTS PASSED
```

Expected figure-generation message:

```text
RERUN V2 MINI HEATMAP FIGURE PASSED
```

## Interpretation guardrails

- Use RMST as the primary cell value.
- Lower RMST means faster readiness formation.
- Describe the heatmap as a design map, not as a standalone inferential test.
- Keep the dependent variable as time to relational coordination readiness, not R&D performance.
- Event quantiles remain secondary and should only be discussed when estimable.

## Suggested caption

Figure X. Mini heatmap of time to relational coordination readiness across boundary-spanner translation capability and role capacity. Cell values report RMST, interpreted as expected time without readiness within the observation horizon; lower values indicate faster relational-readiness formation. The map is a visual synthesis of the translation and workload mechanisms, not a new causal identification strategy. In the tested grid, RMST decreases as translation capability increases and as the same cross-boundary tie budget is distributed across more boundary spanners.
